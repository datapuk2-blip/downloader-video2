import { useForm } from 'react-hook-form';import { Card } from '@/components/ui/Card';import { Button } from '@/components/ui/Button';import { useMemberStore } from '@/store/memberStore';import type { OrganizationSettings } from '@/types/member';
export function Settings(){const{settings,setSettings}=useMemberStore();const{register,handleSubmit}=useForm<OrganizationSettings>({defaultValues:settings});const field='w-full rounded-2xl border p-3 dark:border-slate-700 dark:bg-slate-950';return <section className="mx-auto max-w-2xl space-y-5"><h2 className="text-3xl font-black text-primary dark:text-blue-200">Pengaturan</h2><Card><form className="grid gap-4" onSubmit={handleSubmit(setSettings)}><input className={field} placeholder="Nama organisasi" {...register('namaOrganisasi')}/><input className={field} placeholder="URL Logo" {...register('logo')}/><textarea className={field} placeholder="Alamat" {...register('alamat')}/><input className={field} placeholder="Nomor WhatsApp" {...register('whatsapp')}/><input className={field} placeholder="Email" {...register('email')}/><label className="flex items-center gap-2"><input type="checkbox" {...register('darkMode')}/> Dark Mode</label><Button>Simpan Pengaturan</Button></form></Card></section>}
