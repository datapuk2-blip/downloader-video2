import type { ButtonHTMLAttributes } from 'react';import { cn } from '@/utils/cn';
export function Button({className,variant='primary',...props}:ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'ghost'|'danger'|'accent'}){const v={primary:'bg-primary text-white hover:bg-primary/90',secondary:'bg-secondary text-white hover:bg-secondary/90',ghost:'bg-transparent hover:bg-slate-100 dark:hover:bg-slate-800',danger:'bg-danger text-white hover:bg-danger/90',accent:'bg-accent text-slate-950 hover:bg-accent/90'}[variant];return <button className={cn('inline-flex items-center justify-center gap-2 rounded-xl px-4 py-2 text-sm font-semibold transition disabled:opacity-50',v,className)} {...props}/>}
