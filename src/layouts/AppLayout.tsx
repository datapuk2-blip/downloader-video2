import { NavLink, Outlet } from 'react-router-dom';import { FaChartBar,FaCog,FaHome,FaPlus,FaSearch,FaUsers } from 'react-icons/fa';import { motion } from 'framer-motion';import { useMemberStore } from '@/store/memberStore';
const nav=[['/','Dashboard',FaHome],['/anggota','Data Anggota',FaUsers],['/tambah','Tambah Anggota',FaPlus],['/pencarian','Pencarian',FaSearch],['/laporan','Laporan',FaChartBar],['/pengaturan','Pengaturan',FaCog]] as const;
export function AppLayout(){const settings=useMemberStore(s=>s.settings);return <div className="min-h-screen md:flex"><aside className="sticky top-0 z-20 bg-primary text-white md:min-h-screen md:w-72"><div className="p-5"><h1 className="text-2xl font-black">{settings.namaOrganisasi||'DATA SP'}</h1><p className="text-sm text-blue-100">Data Anggota Serikat Pekerja</p></div><nav className="flex gap-2 overflow-x-auto px-3 pb-3 md:block md:space-y-2">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`flex shrink-0 items-center gap-3 rounded-2xl px-4 py-3 text-sm font-semibold ${isActive?'bg-white text-primary':'text-blue-50 hover:bg-white/10'}`}><Icon/>{label}</NavLink>)}</nav></aside><main className="flex-1 p-4 md:p-8"><motion.div initial={{opacity:0,y:16}} animate={{opacity:1,y:0}} transition={{duration:.35}}><Outlet/></motion.div></main></div>}
