import React from 'react';import ReactDOM from 'react-dom/client';import { RouterProvider } from 'react-router-dom';import { QueryClient,QueryClientProvider } from '@tanstack/react-query';import dayjs from 'dayjs';import 'dayjs/locale/id';import { registerSW } from 'virtual:pwa-register';import { router } from '@/router';import '@/styles/index.css';import { useMemberStore } from '@/store/memberStore';
dayjs.locale('id');document.documentElement.classList.toggle('dark',useMemberStore.getState().settings.darkMode);registerSW({immediate:true,onNeedRefresh(){console.info('Pembaruan DATA SP tersedia')},onOfflineReady(){console.info('DATA SP siap digunakan offline')}});ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><QueryClientProvider client={new QueryClient()}><RouterProvider router={router}/></QueryClientProvider></React.StrictMode>);
